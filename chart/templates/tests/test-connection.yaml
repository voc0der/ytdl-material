apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ytdl-material.fullname" . }}-test-connection"
  labels:
    {{- include "ytdl-material.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ytdl-material.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
