# Angular 22 compatibility: ngx-videogular peers Angular 20, ngx-avatars peers
# Angular 21, and @angular/build peers Vitest 4 while we test with Vitest 5.
# Keep peer resolution disabled until these constraints can be addressed together.
legacy-peer-deps=true
